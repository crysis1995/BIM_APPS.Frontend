import { createSelector } from 'reselect';
import { ACCEPTANCE_TYPE } from '../../../sites/work_progress/redux/types/constans';

// export const colored_elements = createSelector(
// 	(state) => state.ForgeViewer.colored_elements,
// 	(colored_elements) => colored_elements,
// );

// export const reactPanelContent = createSelector(
// 	(state) => state.ForgeViewer.panel_content,
// 	(state) => state.Odbiory.OdbioryComponent.active_acceptance_type,
// 	(panel_content, active_acceptance_type) => {
// 		if (active_acceptance_type === ACCEPTANCE_TYPE.MONOLITHIC) {
// 			return panel_content;
// 		} else {
// 			return null;
// 		}
// 	},
// );
