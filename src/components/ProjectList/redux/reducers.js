import types from './types';


const INITIAL_STATE = {

}

const projectListReducer = (state = INITIAL_STATE, action) => {
      switch (action.type) {
            case types.GET_TOKEN:
                  return
      }
}