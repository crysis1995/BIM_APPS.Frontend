// import React, { PropsWithChildren } from 'react';
// import { ArrowUp } from './Arrow.Up';
// import { ArrowDown } from './Arrow.Down';
//
// export function Sorting(
// 	props: PropsWithChildren<{ options: { key: string; asc: boolean } | null; headerKey: string | undefined }>,
// ) {
// 	if (props.options === null || props.options.key !== props.headerKey) return <>{props.children}</>;
// 	return (
// 		<>
// 			{props.children}
// 			{props.options.asc ? <ArrowUp /> : <ArrowDown />}
// 		</>
// 	);
// }
export {}